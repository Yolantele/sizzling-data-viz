import React from 'react'
import { Title } from '@devexpress/dx-react-chart-material-ui'

const TitleText = (props) => (
  <Title.Text {...props} style={{ marginRight: '120px', marginBottom: 100 }} />
)
export default TitleText
