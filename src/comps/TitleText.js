import React from 'react'
import { Title } from '@devexpress/dx-react-chart-material-ui'

const TitleText = (props) => <Title.Text {...props} style={{ marginRight: '120px' }} />
export default TitleText
